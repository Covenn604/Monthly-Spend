# Required: choose a unique password with at least 12 characters.
APP_PASSWORD=
APP_IMAGE=ghcr.io/covenn604/monthly-spend:latest
APP_PORT=8085
# Numeric host user/group IDs with write access to your data directory.
PUID=10001
PGID=10001
CURRENCY=CAD
TZ=America/Vancouver
# Set true only when accessing the app through HTTPS.
COOKIE_SECURE=false
